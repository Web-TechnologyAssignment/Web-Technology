extends layout

block content
  h1= title
  p Welcome to #{title}
  form(name="userlogin", action="/",method="GET")
    input(type="text",name="query")
    input(type="submit",value="submit")

  ul
    for tweet in tweets
      li= tweet
    else
      li Tweet not found!